[project]
name = "maze-solver"
version = "0.1.0"
description = "Boot.dev course: Build a Maze Solver in Python"
readme = "README.md"
authors = [
    { name = "Przemek Lewicki", email = "przemyslaw.lewicki.dev@proton.me" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
maze-solver = "maze_solver:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
